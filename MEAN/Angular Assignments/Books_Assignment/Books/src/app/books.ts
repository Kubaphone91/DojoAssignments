export class Book{
    _id: number;
    title: string;
    year: number;
    pages: number;
    author: string;
    publisher: string;

    // constructor(){
    //   this._id = Math.floor(Math.random() * 1000);
    // }
}
